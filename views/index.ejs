<!DOCTYPE html>
<html lang="en">

<!-- this is the main page for music and tables-->
<head>
    <title>Music Store</title>
    <link href="/css/style.css" rel="stylesheet" type="text/css">
</head>

<body>
    <div class="header">
        <img src="/images/image.png" class="left" alt="Music Notes">
        <img src="/images/image.png" class="right" alt="Music Notes">
        <h1><b>Music Store</b></h1>
        <h2>Your World of Escape</h2>
    </div>
    <div class="body"><br>
      <!-- space that the table occupies, and the words appearing in the table-->
        <table align = "center" class="table">
            <thead>
                <tr>
                    <th></th>
                    <th>SongID</th>
                    <th>SongName</th>
                    <th>ArtistID</th>
                    <th>YearReleased</th>
                    <th></th>
                </tr>
            </thead>
<!-- Where our delete song button comes from, as well as the edit song button-->
            <tbody>
                <% for (var iCount = 0; iCount < MusicLibrary.length; iCount++) {%>
                <tr>
                    <td>
                        <form action="/deleteSong/<%= MusicLibrary[iCount].SongID %>" method="post">
                            <input class="button" type="submit" name="delete" value="Delete">
                        </form>

                    </td>
                    <td><%= MusicLibrary[iCount].SongID %></td>
                    <td><%= MusicLibrary[iCount].SongName %></td>
                    <td><%= MusicLibrary[iCount].ArtistID %></td>
                    <td><%= MusicLibrary[iCount].YearReleased %></td>
                    <td>
                        <form action="/editsong" method="get">
                            <input type="submit" name="edit" value="Edit">
                        </form>
                    </td>
                </tr>
                <% }%>
            </tbody>
        </table>
        <br><br>
        <!-- start over and add song to playlist options -->
        <form action="/addsong" method="get">
            <input class="button" type="submit" name="addSong" value="Add Song">
        </form>

        <form action="/startover" method="post">
            <input class="button" type="submit" name="startOver" value="Start Over">
        </form>
    </div>
</body>

</html>